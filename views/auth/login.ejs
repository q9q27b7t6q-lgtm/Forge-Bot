<%- include('../partials/head') %>
<section class="page-hero" style="justify-content:center;text-align:center">
  <div>
    <p class="eyebrow"><%= t('auth.login_eyebrow') %></p>
    <h1><%= t('auth.login_title') %></h1>
    <p class="muted"><%= t('auth.login_lead') %></p>
  </div>
</section>
<section class="card glass narrow">
  <% if (error) { %>
    <div class="alert alert-error" role="alert"><%= error %></div>
  <% } %>
  <form method="post" action="/login" class="form">
    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
    <label>
      <%= t('auth.email') %>
      <input type="email" name="email" required autocomplete="email" maxlength="254"
             value="<%= escapeHtml(email) %>" />
    </label>
    <label>
      <%= t('auth.password') %>
      <input type="password" name="password" required autocomplete="current-password" minlength="8" maxlength="128" />
    </label>
    <button type="submit" class="btn btn-primary btn-block btn-glow"><%= t('auth.login_btn') %></button>
  </form>
  <p class="muted center"><%= t('auth.no_account') %> <a href="/register"><%= t('auth.signup_link') %></a></p>
</section>
<%- include('../partials/foot') %>
